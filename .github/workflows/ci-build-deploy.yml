name: CI / Build & Deploy

on:
  push:
    branches: [main]

permissions:
  packages: write # Grants permission to push to GHCR

jobs:
  migrate:
    runs-on: ubuntu-latest
    environment: production
    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2

      - run: bun install

      - run: bunx drizzle-kit generate

      - run: bunx drizzle-kit migrate

  build-publish:
    needs: migrate
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2

      - run: bun install

      - name: Set up QEMU (for multi-arch builds) # Optional, drop if not needed
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx # Prepare Buildx builder
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR # Secure registry login
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Set lowercase repository owner and name
        run: |
          echo "REPO_OWNER_LC=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_ENV
          echo "REPO_NAME_LC=${GITHUB_REPOSITORY##*/}" >> $GITHUB_ENV
          echo "REPO_NAME_LC=${REPO_NAME_LC,,}" >> $GITHUB_ENV

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ghcr.io/${{ env.REPO_OWNER_LC }}/${{ env.REPO_NAME_LC }}:${{ github.sha }}

  deploy:
    needs: build-publish
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Trigger Render deploy
        uses: render-examples/render-deploy-action@v2
        with:
          api_key: ${{ secrets.RENDER_API_KEY }}
          service_id: ${{ secrets.RENDER_SERVICE_ID }}
          image: ghcr.io/${{ github.repository_owner }}/${{ github.repository }}:${{ github.sha }}
          wait_for_deploy: true
